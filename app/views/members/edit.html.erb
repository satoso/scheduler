<%= form_for @member do |form| %>
  <%= render 'form_table', form: form %>
  <p>
    <%= form.button 'Save', class: 'btn btn-sm btn-success' %>
    <%= link_to 'Delete', member_path(@member), method: :delete, 
      data: { confirm: 'メンバーを削除します。' }, class: 'btn btn-sm btn-danger' %>
  </p>
<% end %>
